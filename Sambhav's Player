<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sambhav's Player</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@900&display=swap" rel="stylesheet">
  <style>
    body {
      background: url('https://zdblogs.zohowebstatic.com/sites/tech-talk/files/2020-09/cloud.jpg') no-repeat center center fixed;
      background-size: cover;
      margin: 0;
      padding: 0;
      font-family: 'Orbitron', sans-serif;
      color: #ffffff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
    }
    h1 {
      font-size: 3em;
      font-weight: 900;
      color: #0a2a5e;
      background: linear-gradient(to right, #0a2a5e, #1b3f75, #2c54a1);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-shadow: 2px 2px 10px rgba(10,42,94,0.6);
      margin-bottom: 20px;
    }
    input, button {
      padding: 12px;
      font-size: 16px;
      width: 90%;
      max-width: 400px;
      border-radius: 8px;
      border: none;
      margin-bottom: 10px;
      box-shadow: 0 0 10px rgba(255,255,255,0.1);
    }
    input {
      background-color: #2c2c2c;
      color: #fff;
    }
    input::placeholder {
      color: #aaa;
    }
    button {
      background-color: #00ffc8;
      color: #000;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    button:hover {
      background-color: #00cfa1;
    }
    #player {
      margin-top: 20px;
      width: 90%;
      max-width: 720px;
    }
    iframe {
      width: 100%;
      height: 405px;
      border: none;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
    }
    p {
      font-size: 16px;
    }
  </style>
</head>
<body>
  <h1>🎬 Sambhav’s Player</h1>

  <div id="videoList">
    <button onclick="loadFixedVideo('https://youtu.be/dQw4w9WgXcQ')">🎬 YouTube Demo</button>
    <button onclick="loadFixedVideo('https://mdiskplay.com/s/abc123')">🎬 Mdisk Movie</button>
    <button onclick="loadFixedVideo('https://www.dailymotion.com/video/x7z2z6j')">🎬 Dailymotion Clip</button>
  </div>

  <input type="text" id="videoLink" placeholder="Paste YouTube, Shorts, Insta Reel, Dailymotion, TeraBox/mdisk link">
  <button onclick="loadVideo()">Play Video</button>
  <div id="player"></div>

  <script>
    function loadFixedVideo(link) {
      document.getElementById("videoLink").value = link;
      loadVideo();
    }

    function loadVideo() {
      const url = document.getElementById("videoLink").value.trim();
      let embedCode = "";

      if (url.includes("youtube.com") || url.includes("youtu.be")) {
        let videoId = "";
        if (url.includes("watch?v=")) {
          videoId = url.split("v=")[1].split("&")[0];
        } else if (url.includes("youtu.be/")) {
          videoId = url.split("youtu.be/")[1].split("?")[0];
        } else if (url.includes("shorts/")) {
          videoId = url.split("shorts/")[1].split("?")[0];
        }
        embedCode = `<iframe src="https://www.youtube.com/embed/${videoId}" allowfullscreen></iframe>`;
      } else if (url.includes("dailymotion.com") || url.includes("dai.ly")) {
        let videoId = "";
        if (url.includes("dailymotion.com/video/")) {
          videoId = url.split("/video/")[1]?.split("_")[0];
        } else if (url.includes("dai.ly/")) {
          videoId = url.split("dai.ly/")[1]?.split("?")[0];
        }
        embedCode = `<iframe src="https://www.dailymotion.com/embed/video/${videoId}" allowfullscreen></iframe>`;
      } else if (url.includes("mdiskplay.com") || url.includes("mdisk.me")) {
        embedCode = `<iframe src="${url}" allowfullscreen></iframe>`;
      } else if (url.includes("terabox.com") || url.includes("1024terabox.com")) {
        const fileId = url.split("/s/")[1]?.split("?")[0];
        const cleanId = fileId?.substring(1);
        if (cleanId) {
          const mdiskUrl = `https://mdiskplay.com/terabox/${cleanId}`;
          embedCode = `<iframe src="${mdiskUrl}" allowfullscreen></iframe>`;
        } else {
          embedCode = "<p style='color:red;'>❌ Invalid TeraBox link format.</p>";
        }
      } else if (url.includes("instagram.com/reel/")) {
        embedCode = `<iframe src="https://www.instagram.com/reel/${url.split("/reel/")[1].split("/")[0]}/embed" allowfullscreen></iframe>`;
      } else {
        embedCode = "<p style='color:red;'>❌ Unsupported link format.</p>";
      }

      document.getElementById("player").innerHTML = embedCode;
    }
  </script>
</body>
</html>
